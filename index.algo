ALGORITHM algorithm_name
VAR
    c=:CHAR:="gomycode";
    i=:INTEGER:=0;
    sum=:INTEGER:=0;

BEGIN
  FOR i FROM 0 TO c.length-1  DO
   sum:=sum+1;
   
  END_FOR
write("le nombre de charcatere est"sum)
END